`CKli.Publish.Plugin` is a plugin of the dotnet tool `CKli`. It implements the publication of the NuGet packages
to the NuGet feeds and the generated artifacts (if any) on the "Release" associated to the commit point.











