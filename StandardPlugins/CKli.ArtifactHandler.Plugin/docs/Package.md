`CKli.ArtifactHandler.Plugin` is a plugin of the dotnet tool `CKli` that handles the `$Local/NuGet` and `$Local/Assets`
folders and defines the NuGet related configuration of a stack.

It depends on the `CKli.BranchModel.Plugin` because it participates to the `ContentIssue` detection that applies to
all defined branches.







