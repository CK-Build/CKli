# CKli tests

These tests don't use the `CKli.Testing` package. They use a ad-hoc static `TestEnv` class that provides
almost the same `Remotes/` and `Cloned/` folders management (but not with the exact same implementation).

